<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Cambiar Alias</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: white;
    }

    .form-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-top: 120px;
    }

    .form-box {
      background-color: #d9d9d9;
      padding: 25px;
      border-radius: 15px;
      font-size: 18px;
      width: 80%;
      max-width: 500px;
      box-sizing: border-box;
    }

    .form-box label {
      display: block;
      margin-bottom: 10px;
      font-weight: bold;
    }

    .form-box input[type="text"] {
      width: 100%;
      padding: 12px;
      font-size: 16px;
      border: none;
      border-radius: 5px;
      box-sizing: border-box;
      background-color: white;
    }

    .boton-confirmar {
      margin-top: 60px;
      padding: 15px 40px;
      background-color: #d9d9d9;
      border: none;
      border-radius: 12px;
      font-size: 20px;
      font-weight: bold;
      cursor: pointer;
    }

    .boton-confirmar:hover {
      background-color: #bfbfbf;
    }
  </style>
</head>
<body>

  <form action="/change_alias" method="post" class="form-container">
    <div class="form-box">
      <label for="nuevo_alias">Escriba su nuevo Alias:</label>
      <input type="text" id="nuevo_alias" name="nuevo_alias" placeholder="Alias" required>
    </div>

    <button type="submit" class="boton-confirmar">Confirmar</button>
  </form>

</body>
</html>
