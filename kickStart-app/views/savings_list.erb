<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>JubiPago - Mis Ahorros</title>
  <style>
    /* Mantén tus estilos actuales */
    body {
      font-family: Arial, sans-serif;
      background-color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      padding: 20px 0;
    }
    /* ... (otros estilos) ... */
  </style>
</head>
<body>

  <div class="container">
    <img src="/images/jubipago.png" alt="JubiPago Logo" width="200">
    <h2>MIS AHORROS</h2>

    <% if @savings.any? %>
      <% @savings.each do |saving| %>
        <div class="saving-item">
          <div class="saving-name"><%= saving.description %></div>
          <div class="saving-amount">$<%= "%.2f" % saving.amount %> ahorrados</div>
          <div class="action-buttons">
            <button class="add-button" onclick="location.href='/saving'">Agregar</button>
            <form action="/saving/<%= saving.id %>" method="post" style="display: inline;">
              <input type="hidden" name="_method" value="delete">
              <button type="submit" class="withdraw-button">Retirar</button>
            </form>
          </div>
        </div>
      <% end %>
    <% else %>
      <p>No tienes ahorros creados aún.</p>
    <% end %>

    <div class="new-saving">
      <button onclick="location.href='/saving'">Nuevo Ahorro</button>
    </div>
  </div>

</body>
</html>